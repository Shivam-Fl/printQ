# Stable selectors

Prefer these over anything derived from text or DOM position — text changes with copy edits
and positions change with layout, and both produce failures that waste an attempt.

_Empty. The entries seeded here at install time were a generic demo-app placeholder
(`#new-todo`, `.todo`, etc.) that never matched printQ's actual UI — removed 2026-09-18.
Add real entries here as QA runs confirm selectors against `apps/web`._
