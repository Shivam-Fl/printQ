# PrintQs production release log

Last updated: 2026-09-26 IST. This is the durable handoff for every subsequent launch turn. Read and update it before changing any account, infrastructure, PR, or deployed revision. Record evidence, not inferred success. Never add credentials, reset codes, personal documents, or key values here.

## 2026-09-26 evening: owner-requested free-Render deploy and Razorpay recheck

- Re-fetched GitHub: `master=ff9f99fd2d2e28438fdc531875766c192026e359`, `development=6497d1918783b1bbf432ae0e97ae903b855db78e`. The development exact-SHA CI passed, but the final full matrix and hosted development QA did not. Render still reports `d9319e858e98f2d60160b761402f6c10ea0eae25` live and the `ff9f99f` attempt failed; no production code deploy was claimed.
- In authenticated Render `printq-web` environment, staged and used **Save only** for non-secret `PRINTQ_ENVIRONMENT`, `DATABASE_NAMESPACE`, `FIREBASE_PROJECT_ID`, `GCS_BUCKET`, `GOOGLE_APPLICATION_CREDENTIALS`, `EMAIL_FROM`, and switched staged `EMAIL_PROVIDER=resend` / `STORAGE_DRIVER=gcs`. The Render page still showed the old live SHA after saving. Secret values were not opened or copied. A masked screenshot is local-only at `C:\Users\acer\AppData\Local\PrintQ\evidence\20260926\render-env-staged.png`.
- Authenticated Resend Domains now shows `mail.printqs.com` **verified**. This verifies the domain only, not a delivered password-reset message. Local-only evidence: `C:\Users\acer\AppData\Local\PrintQ\evidence\20260926\resend-domain-verified.png`.
- Production PostgreSQL aggregate query returned **0 admins, 0 verified-and-published shops, 0 shop mandates, 0 commission statements**. It did not select personal records. The missing admin bootstrap hash/email remain a startup guard for a new production revision; do not weaken the guard or claim an onboarded shop.
- A fresh authenticated external Razorpay tab eventually loaded in **Live** mode. Subscriptions Settings shows Card, UPI, and eMandate as **Enabled**, with ₹15,000 as the displayed no-extra-authentication threshold. This corrects the older "UPI AutoPay not enabled" observation for the Subscriptions product only. There are 0 subscriptions. The **only approved website displayed is `https://www.packkar.in`**, not PrintQs; a live API key exists by presence only, but its value was not revealed. This account has not been verified for `printqs.com` or variable weekly B2B commission. Razorpay's official on-demand Recurring Payments docs require feature activation, and UPI mandate registration should use Intent after Collect deprecation. No live key, mandate, payment, webhook, or approved-site setting was changed. A local-only masked-view payment-method screenshot is at `C:\Users\acer\AppData\Local\PrintQ\evidence\20260926\razorpay-subscriptions-settings.png`; no API-key screenshot was taken.
- PR #30 from `codex/weekly-collection-test` merged into `development` as `2d71cbf60a8e5c0caa853bb8c2d9b67f298750c1` after required CI/secret scan passed; post-merge development CI `36249286936` also passed. A failing regression reproduced that frozen weekly statements never bound an eligible TEST mandate, making the existing debit-reservation path unreachable. The patch binds only a valid same-environment TEST mandate; disabled production collection stays unbound. Focused commission tests 11/11, full API 93/93, root JUnit suites 172/172, all-workspace typecheck and production build passed locally. Provider dispatch, webhook settlement, live collection and end-to-end TEST accounting remain incomplete; this patch is not a Razorpay launch.
- [PR #31](https://github.com/Shivam-Fl/printQ/pull/31) merged into `development` as `26c0939dfc27f0828cf8d60f1e93d9d885492079` after its required CI/secret scan passed (run `36250402893`). The recurring webhook now uses only dedicated `RAZORPAY_RECURRING_WEBHOOK_SECRET` and fails closed when missing or malformed; retired student-payment HMAC credentials cannot authorize it. Post-merge development SDL QA run `36250617895` and CI run `36250565688` both passed for that exact SHA. Focused signature/HTTP tests 4/4 and API typecheck passed locally. The full local API suite was attempted twice; this Windows host became severely resource-constrained and unrelated OTP/admin/CORS tests exceeded their 5-second timeouts, so neither interrupted run is counted as a pass. No tests were disabled. The local Docker daemon is currently unavailable; disposable PostgreSQL/Redis, migrations, full suite and simulator must pass protected GitHub CI.
- [PR #32](https://github.com/Shivam-Fl/printQ/pull/32) from `codex/recurring-test-settlement` starts from that development merge. It adds an exact Razorpay order identifier to each collection attempt and a TEST-only signed `payment.captured` handler that can post one PostgreSQL settlement reversal after matching the order, mandate customer, currency and paise. It does **not** dispatch a Razorpay order/debit, set an attempt's order ID, or enable production money; provider TEST end-to-end is still impossible until that adapter and credentials exist. Local focused commission/webhook tests passed 11/11, Prisma schema validation, API typecheck and build passed; required PR CI is pending. A local full API run was interrupted after unrelated OTP/admin timeouts under Windows contention and is not counted as passing. No production deployment followed this work.

## Fresh phase 0–2 audit and production deploy gate (2026-09-26)

The external Chrome Render session was authenticated; claiming an existing tab timed out, but a fresh external tab worked and independently confirmed Free compute, the old live SHA, the failed newer deploy, and Auto-Deploy Off. Authenticated Render CLI and GitHub CLI supplied structured inventory in [PHASE_0_2_AUDIT.md](outputs/printqs-release-audit-20260926/PHASE_0_2_AUDIT.md). The Render billing page showed $0.00 September charges/projected at inspection, 84.43/750 free instance-hours used, and no card on file; this is not a promise of future zero cost. Production still serves live `d9319e8` (`dep-daqjk73l550s73bp7u8g`); `ff9f99f` failed and is not live. Free production PostgreSQL expires **2026-10-07**. A custom-format database baseline backup was created outside Git, ACL restricted, and successfully restored into an isolated local PostgreSQL 18 database; 28 finished migrations and aggregate row counts matched, the temporary restore database was dropped, and the original dump SHA stayed unchanged. Managed backup/PITR and offsite durability remain open. Production DB has 12 draft/unpublished shops, 0 active campuses, 8 student records and 83 jobs. **The old live public shop API nevertheless lists all 12 draft shops (8 obvious test/demo/QA), all offline**; this is an observed launch blocker, not a source-only inference.

Render `printq-web` was `autoDeployTrigger=commit`, so a GitHub failed check could still produce a deploy attempt. It is now **`off`**, verified through Render CLI and external Chrome, with no new deploy triggered. `render.yaml` in `development` mirrors this exact-SHA manual promotion policy; only `render deploys create srv-dafems740ujc73b080e0 --commit <verified-sha> --wait` should promote after development QA. Protected PRs #26, #25, #27, and #28 merged into `development` after exact-head checks passed. Last verified code candidate `d8b530ad0ff7e7fe45bdb6cd8377969df4537760` passed post-merge [CI run 36246828601](https://github.com/Shivam-Fl/printQ/actions/runs/36246828601), including disposable PostgreSQL/Redis, migrations, tests, simulator and secret scan; a documentation-only merge may advance the branch head, so recheck it before any release. `master` and the live Render revision remain unchanged. The regression-tested master release-source guard is on `development` but **not yet on `master`**, so it is not enforceable against a master-targeted PR from an old branch until a protected release or a separate guard-only bootstrap reaches master. The exact-SHA QA-evidence gate is also missing. Phase 2 remains partial: the development project/bucket exist, but no hosted dev API/DB/Redis/WIF deployment exists; the Render workspace's single free PostgreSQL/Key Value slots are already occupied by production-named resources. No paid development infrastructure was created or requested.

## Latest local QA (2026-09-26)

Docker Desktop now works. An isolated local PostgreSQL/Redis Compose project (`printq-local-qa-20260926`) and `printq_test` database passed 29/29 migrations, complete build/typecheck, 168 package tests (zero failures) and the 93-assertion print-agent simulator. Local browser smoke covered the landing, verified-shop directory, shop sign-in/recovery, and a complete console-mock student signup → home → both New print buttons → reload → logout journey. A dead `/s/demo` link and outdated online-payment copy were reproduced, regression-tested, corrected, and committed as `5e9cdbf6e6178770e8fe829306049d8a0042a081` on PR #25; the updated landing was rechecked in the browser. Four JUnit files, the initial configuration-failure report, commands, exact limitations and open cases are in [LOCAL_QA_STATUS.md](outputs/printqs-local-qa-20260926/LOCAL_QA_STATUS.md). The initial dev Compose port maps were LAN-exposed; they have been changed and regression-tested as loopback-only. The isolated QA containers are stopped after the run, with volumes preserved; other-project containers were untouched. This is **local mock-provider QA only**: hosted development, final 164-case matrix, real providers, physical printer, and production-safe subset are not passed. The optional MinIO image pull failed, so S3-compatible local storage was not covered. No production infrastructure or money was changed by this QA run.

## Decisions and release identity

- `master` is the user-selected production source; `development` is integration. `main` is unused. Changes go through feature PRs and CI; the owner authorized in-scope PR merges.
- The owner chose to keep the existing Render web, PostgreSQL, and Key Value services on **free** plans for now. Do not silently upgrade them. Development servers must be free/near-zero cost; paid production infrastructure is acceptable after a concrete cost check.
- Render is the requested near-term API host, with the existing dedicated `printqs-production` Firebase project for student identity. This is a user-directed pilot deviation from the canonical plan's Cloud Run target, not proof that the architecture is launch-complete.
- An immediate, separate owner confirmation is still required **immediately before** enabling live Razorpay keys, creating a production mandate, or any real shop debit. General merge/deploy authorization is not that confirmation.
- The source root `C:\Users\acer\Desktop\startup\printQ` has user-owned changes and must remain untouched. Worktree `C:\Users\acer\Desktop\startup\printQ-prod-env-fix`, branch `codex/render-prod-env-fix`, is isolated.

## Actual deployment and PR state

| Item | Verified state | Evidence / next action |
| --- | --- | --- |
| GitHub production branch | `origin/master` was `ff9f99fd2d2e28438fdc531875766c192026e359` when the Render failure was diagnosed. | Re-fetch before merging; this SHA passed master CI but **did not deploy**. |
| Render live API | Service `srv-dafems740ujc73b080e0`, `https://api.printqs.com`, was still running `d9319e858e98f2d60160b761402f6c10ea0eae25` (deploy `dep-daqjk73l550s73bp7u8g`). `/healthz` returned 200. | Recheck live SHA after the next deploy. Health alone is not release QA. |
| Failed candidate deploy | `dep-dar65rrncjis73c9iibg` built, applied 28 Prisma migrations, then exited: `STUDENT_AUTH_PROVIDER=firebase requires FIREBASE_PROJECT_ID`. | PR #25 pins the correct project and the existing database namespace; source has other production guards addressed below. Existing DB may now have newer schema than live code. |
| Integrated development candidate | Protected [PR #26](https://github.com/Shivam-Fl/printQ/pull/26), [#25](https://github.com/Shivam-Fl/printQ/pull/25), [#27](https://github.com/Shivam-Fl/printQ/pull/27), and [#28](https://github.com/Shivam-Fl/printQ/pull/28) merged into `development`; last verified code candidate `d8b530ad0ff7e7fe45bdb6cd8377969df4537760`. | PR #28's exact-head checks and the post-merge development [CI run 36246828601](https://github.com/Shivam-Fl/printQ/actions/runs/36246828601) passed. Re-read branch head after documentation changes. This source is **not** a hosted development deployment or a production release. |
| Paid-plan proposal | PR #24 was closed without merge at owner direction. | Do not reopen without a new price/plan decision. |

## Accounts and infrastructure inventory

| Platform / resource | Verified state | Cost or risk |
| --- | --- | --- |
| Render web `printq-web` | Free, combined API/conversion/worker process; `autoDeployTrigger=off` as of this audit, so `master` commits do not automatically deploy. | 512 MB / 0.1 CPU, sleeps after idle, ephemeral filesystem; cannot claim reliable real-order capacity. [Render expressly says free instances are not for production](https://render.com/docs/free). |
| Render PostgreSQL `printq-db` (`dpg-dafem4f40ujc73b059u0-a`) | Free, existing database name `printq_db_f68p`; 28 migrations applied by failed deployment. | 1 GB, no managed backups, scheduled free expiry 2026-10-07; real-order durability gate remains open. |
| Render Key Value `printq-redis` (`red-dafem4740ujc73b0596g`) | Free, existing production Redis. | 25 MB, memory-only; queue state can be lost on restart. Real print-order reliability gate remains open. |
| Firebase production | Dedicated project `printqs-production` exists. Render already had `FIREBASE_AUTH_API_KEY` by name; PR #25 pins `FIREBASE_PROJECT_ID=printqs-production`. | Do not use development Firebase users, credentials, or project. Auth/App Check/realtime/FCM live flows are not yet re-QA'd. |
| Google Cloud Storage production | Created `printqs-production-private` in `printqs-production`, Mumbai `asia-south1`, Standard, uniform access, public access prevention enforced, no soft delete/versioning/retention. Bucket initially empty. | Usage-based: Console showed $0.023/GB-month storage, plus operations/egress. Runtime deletion and signed-URL invalidation still need live QA. |
| Google service identity | Created `printqs-render-storage@printqs-production.iam.gserviceaccount.com` with `Storage Object Admin` on **that bucket only**, not the whole project. JSON key was put in Render Secret Files as `gcs-production-service-account.json`; the temporary local download was removed. | Rotate/revoke if compromise suspected. Never put JSON in Git or logs. Render ADC points to `/etc/secrets/gcs-production-service-account.json` in the pending Blueprint. |
| Resend | A sending-only production API key was created and saved as a protected Render `RESEND_API_KEY` variable without printing it. Domain `mail.printqs.com` added. On 2026-09-26 the in-app tab showed a login page and a Chrome domain-page attempt timed out; no authenticated sender status could be read. | Account plan/usage not verified. [Published Free tier](https://resend.com/pricing): $0, 3,000 emails/month, 100/day, 3 domains; paid Pro starts at $20/month. Last observed sender state was **pending**; DNS resolution alone is not verification. |
| GoDaddy DNS | `printqs.com` uses GoDaddy nameservers. Added exactly three mail-subdomain records: `rsend.mail` CNAME, `send.mail` CNAME, and `resend._domainkey.mail` TXT. Existing web/API DNS records were not edited. `Resolve-DnsName` found all three on 2026-09-26 IST. | Resend itself last reported `pending`; public DNS resolution is not equivalent to its verified status. Recheck there. |
| Razorpay | Production account/login and UPI AutoPay approval not reverified in this turn. No live keys, mandate, or debit enabled by this work. | Student checkout must remain pay-at-shop; current source rejects `SHOP_COLLECTION_MODE=live`. Do not bypass. |
| Development GCP | `printqs-development` Firebase project, private empty GCS bucket, empty Artifact Registry, VPC/PSA exist. Expensive Cloud SQL and Memorystore dev instances were deleted 2026-09-25 after explicit owner approval. No Cloud Run workload is deployed. | No $50+/month development server should be recreated. Check small usage charges separately. |

## This worktree's verified changes

- PR #25's first commit pinned production Firebase project, Render production environment, and the exact existing production database namespace. A Blueprint regression test passed; all PR checks passed at that original SHA.
- The Blueprint now specifies Resend and private GCS, with secret-backed `RESEND_API_KEY`, `ADMIN_BOOTSTRAP_EMAIL`, and `ADMIN_BOOTSTRAP_PASSWORD_HASH`. `node --test scripts/verify-render-production.test.mjs`: 2/2 pass; `render blueprints validate ./render.yaml`: valid; `git diff --check`: pass. These changes merged into `development`, but are **not deployed** to any hosted API or production.
- Render's production storage JSON secret file and Resend key were saved with **Save only**; they were not used to deploy the old source.
- The merged development source adds a separate platform-admin email recovery flow (15-minute code, per-admin send cap, five-attempt limit, one-time consume, session-version revocation, audit event) and a matching login-page recovery state. It does **not** set or disclose a bootstrap credential, and is not deployed. Verification on 2026-09-26: Prisma generate/validate, API/web typechecks and production builds, API 88/88 tests with `npx vitest run --maxWorkers=2`, web 17/17 tests, focused admin recovery 2/2, token 2/2, plus post-merge CI migrations, full test suite and simulator passed. Hosted development QA remains open.

## Open gates, in priority order

1. Verify Resend DNS/domain status and a real password-recovery email. The domain is pending; do not claim it is verified.
2. Configure a separately authenticated platform admin without disclosing a plaintext password; prove the new admin login/recovery and shop verification against a deployed environment. `ADMIN_BOOTSTRAP_*` values are not yet set in Render. The recovery code exists only on the unmerged PR branch.
3. Later, after hosted development QA, promote the exact verified candidate through a protected release PR, confirm Blueprint and out-of-band Render variables/secret file survive sync, and verify Render's exact deployed SHA and startup logs. A failed deploy must not be called live.
4. Run production-safe Firebase/auth, private upload/deletion, queue, pay-at-shop, printer simulator, and email journeys. Real physical printer acceptance is not evidenced.
5. Resolve free production Postgres expiry/no-backup and memory-only queue risk before accepting genuine paid print orders. A successful `/healthz` is insufficient.
6. Complete isolated development deployment and fresh full 100+ case release matrix bound to exact SHA. No such final matrix pass exists yet.
7. Complete Razorpay TEST weekly shop-commission reconciliation, product/use-case approval, and obtain immediate explicit live-money confirmation before any live cutover.

## Rollback and safety

The live Render revision remains `dep-daqjk73l550s73bp7u8g` until a new deploy is independently verified. If a new deploy fails, Render has been serving its prior successful revision; verify this rather than assuming. Revert bad source through a protected feature PR, not a direct `master` push. Database migrations already applied by a failed deploy are **not** automatically rolled back; inspect schema compatibility before any rollback. No production mandate or live debit has been initiated.

2026-09-26 read-only recheck: `origin/master` is still `ff9f99fd2d2e28438fdc531875766c192026e359`; Render lists `dep-dar65rrncjis73c9iibg` as `update_failed` and `dep-daqjk73l550s73bp7u8g` as `live`; `https://api.printqs.com/healthz` returned HTTP 200 with `{"ok":true}`. This is service availability only, **not** proof the new candidate deployed or is launch-safe.
